# Sandbox CI: package smoke test (no private @netcracker registry required).

package(default_visibility = ["//visibility:public"])

sh_test(
    name = "test",
    size = "small",
    srcs = ["ci_smoke_test.sh"],
    args = ["$(location package.json)"],
    data = ["package.json"],
)
