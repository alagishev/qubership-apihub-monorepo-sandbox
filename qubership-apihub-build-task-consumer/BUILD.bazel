# Sandbox CI: package smoke test (no private npm registry / hermetic node_modules yet).

package(default_visibility = ["//visibility:public"])

sh_test(
    name = "test",
    size = "small",
    srcs = ["ci_smoke_test.sh"],
    args = ["$(location package.json)"],
    data = ["package.json"],
)
